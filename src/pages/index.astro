---
import Layout from '../layouts/Layout.astro';
import info from '../data/sample.json'
import Link from '../components/Link.astro';
import Header from '../components/HeroSection/Header.astro';
---
<Layout title={info.name}>

  <Header/>
    
  
</Layout>

<!-- GSAP Animation Script -->
<script>
  import { gsap } from "gsap";
  import { SplitText } from "gsap/SplitText";

  document.addEventListener('DOMContentLoaded', function() {

    const splitText = new SplitText("#logo", { type: "words, chars, lines",
      linesClass: "line",
      wordsClass:"word",
      mask:'lines'
     });
    const logoImg = new SplitText("#main-logo", { type: "words, chars, lines",
      linesClass: "line",
      wordsClass:"word",
      mask:'lines'
     });
     const mainTitle=new SplitText("#main-title", { type: "words, chars, lines",
      linesClass: "line",
      wordsClass:"word",
      mask:'lines'
     });
    
    // 🎨 1. Animation للـ Navigation Bar
    gsap.fromTo("#navbar", 
      { y: -100, opacity: 0 }, 
      { y: 0, opacity: 1, duration: 1, ease: "power2.out" }
    );

    // 🎨 2. Animation للـ Logo
    gsap.fromTo(splitText.lines, 
      { overflow: "hidden",y:100 }, 
      { y:0,duration: 1.2, delay: 0.5,  }
    );

    // 🎨 3. Animation للـ Menu Items
    gsap.fromTo(".menu-item", 
      { x: -50, opacity: 0 }, 
      { 
        x: 0, 
        opacity: 1, 
        duration: 0.8, 
        delay: 0.8,
        stagger: 0.2, 
        ease: "power2.out" 
      }
    );

    // 🎨 4. Animation للـ Main Logo 
    gsap.fromTo("#main-logo", 
      { scale: 0, rotation: 360 }, 
      { scale: 1, rotation: 0, duration: 1.5, delay: 1.3, ease: "elastic.out(1, 0.5)" }
    );

    // 🎨 5. Animation للـ Status Card
    gsap.fromTo("#status-card", 
      { x: 100, opacity: 0 }, 
      { x: 0, opacity: 1, duration: 1, delay: 1.6, ease: "power2.out" }
    );

    // 🎨 6. Animation للـ Hero Content
    const heroTimeline = gsap.timeline({ delay: 2 });
    
    heroTimeline
    .fromTo("#date", 
      { y: 30, opacity: 0 }, 
      { y: 0, opacity: 1, duration: 0.8, ease: "power2.out" }, 
      "-=0.5" 
    )
    .fromTo("#role", 
      { y: 30, opacity: 0 }, 
      { y: 0, opacity: 1, duration: 0.8, ease: "power2.out" }, 
      "-=0.3"
    ).fromTo(mainTitle.lines, 
        { y: 100, opacity: 0 }, 
        { y: 0, opacity: 1, duration: 1, ease: "power2.out" }
      )

    // 🎨 7. Hover Animation للـ Menu Items
    document.querySelectorAll('.menu-item').forEach(item => {
      item.addEventListener('mouseenter', () => {
        gsap.to(item, { scale: 1.1, duration: 0.3, ease: "power2.out" });
      });
      
      item.addEventListener('mouseleave', () => {
        gsap.to(item, { scale: 1, duration: 0.3, ease: "power2.out" });
      });
    });

    // 🎨 8. Continuous Animation للـ Main Logo 
    gsap.to("#main-logo", {
      rotation: 360,
      duration: 20,
      repeat: -1, 
      ease: "none"
    });

  });
</script>