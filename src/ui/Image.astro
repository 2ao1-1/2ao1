<!-- ---
import { Image } from 'astro:assets';
import info from '../data/sample.json';
const { 
  src, 
  alt, 
  width = 400, 
  height = 300, 
  class: className = '', 
  loading = 'lazy',
  ...rest 
} = Astro.props;
---

<Image 
  src={src}
  alt={alt}
  width={width}
  height={height}
  class={className}
  loading={loading}
  {...rest}
/> -->