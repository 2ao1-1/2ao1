---
import Link from "../Link.astro";
import info from '../../data/sample.json'
---
<nav class="flex items-center justify-between mb-4 border-b p-2 w-full" id="navbar">
    <div class="flex justify-between md:justify-start md:items-start items-center gap-4 px-4 w-full md:w-1/4">
        <h3 class="text-xs md:text-2xl md:w-1/2 cursor-default font-mainHead" id="logo">
          <Link href="/">{info.name}</Link>
        </h3>
        <button class="md:hidden">=</button>

        <ul class="hidden md:block border-l pl-2 overflow-hidden" id="menu">
          {info.siteMap.map((site, index) => (
            <li class="text-sm hover:underline duration-400 transform menu-item" data-index={index}>
              <Link href={site.path}>{site.pageName}</Link>
            </li>
          ))}
        </ul>
    </div>
      
    <div class="hidden md:block font-subhead">
        <p id="date">{new Date().toLocaleDateString('en-US', {
            weekday: 'long',
            year: 'numeric',
            month: 'long',
            day: 'numeric'
            })}
        </p>

        <p id="date">{new Date().toLocaleTimeString('en-US', {
            hour: '2-digit',
            minute: '2-digit',
            second: '2-digit'
            })}
        </p>
    </div>
</nav>